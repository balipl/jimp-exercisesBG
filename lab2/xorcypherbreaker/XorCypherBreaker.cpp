//
// Created by bartek on 16.03.17.
//

#include "XorCypherBreaker.h"

using namespace std;

string XorCypherBreaker(const vector<char> &cryptogram,
                             int key_length,
                             const vector<string> &dictionary){
/*

char cipher_1;
char cipher_2;
char cipher_3;
    string a,b,c;
    string szyfr;
    string odszyfrowane;
*/
 /*   for (int i = 0; i < 26 ; i=i+1) {
        cipher_1 = 'a'+i;
        for (int j = 0; j < 26; j=j+1) {
            cipher_2 = 'a'+j;
            for (int k = 0; k < 26; k=k+1) {
                cipher_3 = 'a'+k;
                szyfr = cipher_1;
                szyfr += cipher_2;
                szyfr += cipher_3;

                odszyfrowane = "";*/
               /* for (int l = 0; l < cryptogram.size()-3 ; l=l+3) {

                    a=cryptogram[l]^szyfr[0];
                    b=cryptogram[l+1]^szyfr[1];
                    c=cryptogram[l+2]^szyfr[2];
                    odszyfrowane+=a;
                    odszyfrowane+=b;
                    odszyfrowane+=c;
*/

                   // }
                //size_t znalezionaPozycja = odszyfrowane.find( "for" );
                //if( znalezionaPozycja != std::string::npos )
                //{//cout << "DUPA";
               // for (int m = 1; m < odszyfrowane.size(); ++m) {
                    //cout << odszyfrowane[m];
                //}
                 //   cout << endl;




       return "xD"; }


